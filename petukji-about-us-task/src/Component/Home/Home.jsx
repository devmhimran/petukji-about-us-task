import { Button } from '@material-tailwind/react';
import React from 'react';

const Home = () => {
    return (
        <div>
            <Button> filled</Button>
        </div>
    );
};

export default Home;